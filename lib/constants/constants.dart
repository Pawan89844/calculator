import 'package:flutter/material.dart';

const kCalculatorStyle = TextStyle(
  color: Colors.black87,
  fontSize: 30,
);

const kCalculatorStyleSymbols = TextStyle(
  color: Color(0xFFF38181),
  fontSize: 30,
);
const kCalculatorAnswer = TextStyle(
  color: Colors.grey,
  fontSize: 20,
);

const kCalculatorStyleSymbols1 = TextStyle(
  color: Colors.teal,
  fontSize: 30,
);

const kBorderRadiusOfButtons = BorderRadius.only(
  topLeft: Radius.circular(20),
  topRight: Radius.circular(20),
);
